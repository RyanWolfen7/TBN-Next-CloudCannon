import CloudCannonVisualEditorConnector from '@cloudcannon/visual-editor-connector';

const useConnector = () =>{ 
    const connector = console.log(CloudCannonVisualEditorConnector)
    
    return connector 
}

export default useConnector
